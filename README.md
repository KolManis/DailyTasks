# DailyTasks API 🚀

REST API для управления задачами на Go с PostgreSQL и Docker.

## Возможности

- Создание задач
- Получение списка задач  
- Отметка задач выполненными
- Удаление задач
- Полная контейнеризация с Docker
- Хранение данных в PostgreSQL

## Быстрый старт

```bash
docker-compose up -d
```

## Структура проекта
```
DailyTasks/
├── main.go                 # Основное приложение
├── Dockerfile              # Образ приложения
├── docker-compose.yml      # Конфигурация сервисов
├── migrations/             # Миграции БД
└── docs/                   # Документация
```
## Таблица методов

| Метод | URL | Описание |
|-------|-----|-----------|
| GET | `/api/todos` | Получить все задачи |
| POST | `/api/todos` | Создать новую задачу |
| PATCH | `/api/todos/:id` | Пометить задачу выполненной |
| DELETE | `/api/todos/:id` | Удалить задачу |